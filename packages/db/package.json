{
  "name": "@packages/db",
  "scripts": {
    "edge:generate:js": "bunx @edgedb/generate edgeql-js --target cjs",
    "edge:generate:interfaces": "bunx @edgedb/generate interfaces",
    "edge:new:migration": "edgedb migration create",
    "edge:migrate": "edgedb migrate"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}